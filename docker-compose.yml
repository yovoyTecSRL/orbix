version: '3.8'

services:
  orbix:
    build: .
    container_name: orbix-ai
    ports:
      - "80:8000"
    env_file:
      - .env
    restart: always
    volumes:
      - ./orbix_config.json:/app/orbix_config.json:ro
